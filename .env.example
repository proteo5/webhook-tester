# Webhook Tester Configuration

# Default server configuration
PORT=3000
NODE_ENV=development

# Optional: Enable detailed header logging
SHOW_ALL_HEADERS=false

# Optional: Enable request body size limit (in MB)
BODY_SIZE_LIMIT=10